{
  "name": "@pact-network/wrap",
  "version": "0.3.0",
  "private": true,
  "description": "Network-core fetch-call wrap library: any interface (Pact Market, BYO SDK, x402 facilitator, etc.) consumes this to insure a fetch call against Pact Network.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@pact-network/classifier": "workspace:*"
  },
  "peerDependencies": {
    "@google-cloud/pubsub": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "@google-cloud/pubsub": {
      "optional": true
    }
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
